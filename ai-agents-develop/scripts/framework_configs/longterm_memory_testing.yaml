framework_id: Longterm-Memory-Testing
framework_name: Longterm Memory Testing
framework_description: Testing the longterm memory system.

execution:
  auto_create: true
  name: 2025 Oct
  type: On Demand

controls:
  - name: Longterm Memory Testing
    instructions: |
      **SKIP COMPLIANCE CHECK**
      Answer the question base on the longterm memory.

    action_list:
      - action_name: Longterm Memory Testing
        action_prototype_name: SupervisorAgent
        order: 0
        control_variables:
          task_description:
            value_type: primitive
            value: |
              Answer the question base on the longterm memory.

        independent_variable_schema:
          additional_description:
            value_type: args
            args_schema:
              type: string
              example: "Today is ..."
              description: Primary task description for the supervisor agent

    entities:
      - name: "Test"
        independent_variables:
          - additional_description: |
              How to request an evidence in Archer system?
          # - {}
          # - {}
          # - {}
